myvenv\Scripts\activate
python manage.py runserver

python manage.py migrate #Добавляем БД

python manage.py startapp portrets #Создаем приложение

#Идем в Settings:
    INSTALLED_APPS = [
        'django.contrib.admin',
        'django.contrib.auth',
        'django.contrib.contenttypes',
        'django.contrib.sessions',
        'django.contrib.messages',
        'django.contrib.staticfiles',
        'portrets',
    ]

#Создаем модели
...
python manage.py makemigrations portrets
python manage.py migrate portrets

#Добавляем админку
blog/admin.py

    from django.contrib import admin
    from .models import Post

    admin.site.register(Post)

#Создать суперЮзера
python manage.py createsuperuser

python manage.py runserver


#Git-init
cd U:\WORK\portrethit

#1st time
git init
git config --global user.name "JekaDeka"
git config --global user.email SpecialForWarcraft@gmail.com

git status
git add -A .
git commit -m "portrethit"


git remote add origin https://github.com/JekaDeka/portrethit.git
git push -u origin master